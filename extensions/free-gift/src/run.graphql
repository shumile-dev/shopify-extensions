query RunInput {
  cart {
    lines {
      id
      type: attribute(key: "type") {
        value
      }
      cost {
        totalAmount {
          amount
          currencyCode
        }
      }
      
    }
  }
}
